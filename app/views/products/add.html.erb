<h1> Root Page </h1>

<%= form_for @product, url: products_add_path,  method: :post do |f| %>
<%= f.label :name %>
<%= f.text_field :name, :id => "product" %>
<%= f.submit "add to cart"  %>
<% end %>

<% cart.each do |product| %>
  <%= product %>
<% end %>

